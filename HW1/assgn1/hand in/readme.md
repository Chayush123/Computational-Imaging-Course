# README

We implemented the pipeline in a jupyter notebook. We ran dcraw seperately and added its output manually in the header of the notebook. It produces all the outputs in the same folder.

THe white balance is controlled by a keyword set in the header and also modifies the file output.